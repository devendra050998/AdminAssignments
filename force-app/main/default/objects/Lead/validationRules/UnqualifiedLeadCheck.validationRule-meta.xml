<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>UnqualifiedLeadCheck</fullName>
    <active>true</active>
    <errorConditionFormula>AND(
ISPICKVAL(Status, &quot;Unqualified&quot;),
ISBLANK(Reason__c),
NOT(ISNEW()),
NOT(ISCLONE())
)</errorConditionFormula>
    <errorMessage>make sure that the validation rule shouldn’t trigger for cloned and new records.</errorMessage>
</ValidationRule>
